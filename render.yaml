services:
  - type: web
    name: multilingual-ecommerce-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && uvicorn app.main:app --host 0.0.0.0 --port 8000
    plan: free
    region: oregon
    autoDeploy: true
